run: +js_vars
+js_vars:
  +task1:
    sh>: "echo $val"
    val: 1

  +task2:
    sh>: "echo $val"
    val=: ${1 + 1}

  +task3:
    +run_command:
      val=: ${2 + 3}
      command=: echo ${val}
      sh>=: ${command}

