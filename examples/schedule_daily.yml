name: +daily1
_timezone: America/Los_Angeles   # this considers daylight saving time

_schedule:
  daily>: 10:00:00   # daily at 10 AM

+task:
  sh>: echo "This schedule is for ${session_time}"

# See also sla.yml

