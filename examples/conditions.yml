name: conditions
timezone: UTC

+algorithms:
  +try:
    _parallel: true
    +zlib:
      py>: examples.conditions.Algorithm.zlib
    +gzip:
      py>: examples.conditions.Algorithm.deflate
    +bzip2:
      py>: examples.conditions.Algorithm.bzip2
  +decide:
    py>: examples.conditions.Algorithm.decide_algorithm

+show:
  py>: examples.conditions.show_algorithm

